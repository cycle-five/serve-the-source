{
  "name": "serve-the-source",
  "version": "0.1.1",
  "description": "Emit the Markdown source beside every rendered page, and serve it to agents that ask. Zero runtime dependencies.",
  "keywords": [
    "quartz",
    "quartz-plugin",
    "markdown",
    "static-site",
    "agents",
    "llm",
    "content-negotiation"
  ],
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json",
    "./cli": {
      "types": "./dist/cli.d.ts",
      "import": "./dist/cli.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "quartz": {
    "type": "emitter",
    "entry": "ServeTheSource"
  },
  "scripts": {
    "build": "esbuild src/index.ts src/cli.ts src/bin.ts src/frontmatter.ts --platform=node --format=esm --outdir=dist && tsc --emitDeclarationOnly --declaration --outDir dist -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test && npm run build && npm run smoke",
    "smoke": "node test/smoke-dist.mjs"
  },
  "peerDependencies": {
    "@quartz-community/types": ">=0.2"
  },
  "peerDependenciesMeta": {
    "@quartz-community/types": {
      "optional": true
    }
  },
  "devDependencies": {
    "@quartz-community/types": "^0.3.0",
    "@types/node": "^24.10.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cycle-five/serve-the-source.git"
  },
  "bin": {
    "serve-the-source": "./dist/bin.js"
  },
  "engines": {
    "node": ">=18"
  },
  "bugs": {
    "url": "https://github.com/cycle-five/serve-the-source/issues"
  },
  "homepage": "https://github.com/cycle-five/serve-the-source#readme"
}
